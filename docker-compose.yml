services:
  nephelios:
    build: .
    restart: always
    container_name: nephelios
    labels:
      - "com.nephelios.name=nephelios"
    env_file:
      - .env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - "3030:3030"
