version: '3.9'

networks:
  traefik-global-proxy:
    name: "traefik-global-proxy"

services:
  traefik:
    image: traefik:v2.10
    container_name: traefik
    command:
      - --configFile=/etc/traefik/traefik.toml
    ports:
      - "80:80"
      - "443:443"
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./traefik.toml:/etc/traefik/traefik.toml
    labels:
      - "traefik.http.services.traefik.loadbalancer.server.port=8080"
    networks:
      - traefik-global-proxy

  eeevvv:
    image: eeevvv:latest
    container_name: eeevvv
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.eeevvv.rule=Host(`eeevvv.localhost`)"
      - "traefik.http.routers.eeevvv.entryPoints=websecure"
      - "traefik.http.routers.eeevvv.tls=true"
      - "traefik.http.services.eeevvv.loadbalancer.server.port=3000"
    networks:
      - traefik-global-proxy
     